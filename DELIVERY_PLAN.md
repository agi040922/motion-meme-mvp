# Motion Meme MVP Delivery Plan

## 1. 문서 목적

PRD와 TRD를 실제 제작 순서와 운영 방식으로 전환하기 위한 실행 문서다.  
목표는 "무엇을 언제 어떤 기준으로 만들고 검증할지"를 명확하게 만드는 것이다.

현재 실제 구현 반영/검증 상태는 `docs/IMPLEMENTATION_STATUS.md`를 기준으로 추적한다.

## 2. 제작 원칙

- 단일 Next.js 앱으로 간다.
- 핵심 리스크부터 먼저 검증한다.
- 백엔드보다 플레이 경험과 업로드 흐름을 먼저 붙인다.
- SNS 기능은 최소 완성 형태로 빠르게 연결한다.
- 매 단계마다 실제 브라우저에서 동작 검증을 한다.

## 3. 가장 먼저 검증할 것

이 MVP의 핵심 리스크는 아래 4개다.

1. 브라우저에서 포즈 유사도 기반 판정이 충분히 그럴듯하게 동작하는가
2. 밈 오버레이가 카메라 화면 위에 자연스럽게 겹쳐지는가
3. 15초 결과 영상을 브라우저에서 생성하고 업로드할 수 있는가
4. 로그인 후 플레이 -> 업로드 -> 피드 게시 흐름이 매끄러운가

즉, 첫 단계에서 SNS보다 먼저 "플레이 코어 루프"를 붙여야 한다.

## 4. 추천 제작 순서

### Phase 0. 설계 확정

목표:
- 문서 기준을 구현 단위로 내린다.

해야 할 일:
- Supabase 프로젝트 생성
- 환경 변수 목록 확정
- 앱 IA와 라우트 구조 확정
- DB 테이블 초안 확정
- 스테이지 10개 정의 초안 작성
- 밈 자산 규칙과 폴더 구조 확정

산출물:
- DB schema draft
- route map
- component map
- asset convention

### Phase 1. 플레이 코어 프로토타입

목표:
- 로그인 없이도 로컬에서 챌린지 1개가 돌아가는지 먼저 검증

해야 할 일:
- 카메라 연결
- Pose Landmarker 연결
- 스틱/오버레이 표시
- 포즈 유사도 점수 계산
- 성공/실패 판정
- 밈 오버레이 연출 1종 구현
- 15초 결과 영상 녹화/미리보기

완료 기준:
- 로컬에서 1개 스테이지를 실제로 플레이할 수 있음
- 점수와 성공 판정이 보임
- 결과 영상 미리보기가 됨

### Phase 2. 앱 기반 구축

목표:
- 실제 서비스 구조로 진입 가능하게 만듦

해야 할 일:
- Next.js 앱 초기 세팅
- 공통 레이아웃
- Supabase 연결
- Google 로그인
- protected route 처리
- 프로필 기본 생성
- 랜딩 페이지 초안

완료 기준:
- 로그인 가능
- 랜딩에서 로그인 유도 가능
- 로그인 후 플레이 화면 진입 가능

### Phase 3. 스테이지 시스템

목표:
- MVP 게임 구조 완성

해야 할 일:
- 10단계 스테이지 데이터 구조 구현
- 순차 해금 로직 구현
- 스테이지별 점수 저장
- 최고 점수 저장
- 최근 플레이 기록 저장
- 결과 영상과 스테이지 연결

완료 기준:
- 10단계가 순차 해금됨
- 각 스테이지 결과가 저장됨
- 프로필에 최고 점수/최근 기록이 반영됨

### Phase 4. SNS 기능

목표:
- 업로드된 결과물이 실제 콘텐츠가 되게 만듦

해야 할 일:
- 게시물 생성
- 글 작성
- 이미지 업로드
- 플레이 영상 업로드
- 최신순 피드
- 인기순 피드
- 댓글
- 좋아요
- 팔로우

완료 기준:
- 업로드한 게시물이 피드에 노출됨
- 다른 사용자가 상호작용 가능함

### Phase 5. 프로필/랜딩 고도화

목표:
- 서비스가 하나의 제품처럼 보이게 만듦

해야 할 일:
- 프로필 페이지 완성
- 소개글/대표 밈/최근 영상 노출
- 랜딩에 피드 미리보기 반영
- 랜딩 CTA 반복 배치
- 톤앤매너 정리

완료 기준:
- 비로그인 랜딩에서 서비스 이해 가능
- 로그인 전환 퍼널이 동작함

### Phase 6. QA/출시 준비

목표:
- MVP 배포 가능한 수준으로 안정화

해야 할 일:
- 모바일 브라우저 확인
- 권한 거부/실패 케이스 정리
- 업로드 실패 처리
- 성능 최적화
- Storage 비용 점검
- 기본 모더레이션/신고 최소 설계

완료 기준:
- 핵심 사용자 흐름이 끊기지 않음
- 주요 브라우저에서 플레이 가능
- 운영 리스크가 통제 가능함

## 5. 실제 작업 묶음 방식

큰 기능 단위보다 "끊어서 검증 가능한 작업 단위"로 관리하는 게 좋다.

예시:
- `camera-setup`
- `pose-scoring-engine`
- `stage-clear-ui`
- `meme-overlay-renderer`
- `result-video-recorder`
- `google-auth`
- `profile-bootstrap`
- `feed-post-create`
- `feed-sort-latest`
- `feed-sort-trending`
- `comment-system`
- `follow-system`

각 작업은 1~2일 안에 끝나는 크기로 쪼개는 게 좋다.

## 6. 권장 일정 운영

### 1주차

- 앱 뼈대
- Supabase 연결
- Google 로그인
- 카메라/포즈 인식 연결
- 포즈 유사도 1차 점수화

### 2주차

- 스테이지 1~3 구현
- 밈 오버레이
- 결과 녹화/미리보기
- 점수 저장

### 3주차

- 10단계 확장
- 순차 해금
- 프로필
- 업로드 저장

### 4주차

- 피드
- 댓글/좋아요/팔로우
- 랜딩 고도화
- QA/수정

## 7. 제작 과정 관리 방식

### 추천 관리 도구

- 이슈 관리: Linear 또는 GitHub Projects
- 디자인/기획 정리: Notion 또는 문서 폴더
- 개발: GitHub
- 배포: Vercel
- 백엔드: Supabase

### 추천 보드 컬럼

- Backlog
- Ready
- In Progress
- In Review
- QA
- Done

### 이슈 템플릿 예시

- 목적
- 사용자 가치
- 작업 범위
- 완료 조건
- 리스크
- 관련 화면/테이블

## 8. 브랜치/릴리즈 전략

- `main`: 배포 가능한 상태 유지
- `develop`: 통합 브랜치
- 기능별 브랜치:
  - `feature/google-auth`
  - `feature/stage-system`
  - `feature/feed`
  - `feature/profile`

작업이 많지 않다면 `main` + feature branch만으로도 충분하다.

## 9. QA 기준

각 Phase가 끝날 때 아래를 확인한다.

- 실제 브라우저에서 동작하는가
- 모바일에서 최소 사용 가능한가
- 로그인 흐름이 끊기지 않는가
- 점수 저장이 정확한가
- 업로드 후 피드 반영이 되는가
- 성능 저하가 심하지 않은가

## 10. 정의된 완료 기준

MVP 완료는 아래가 모두 충족될 때로 본다.

- Google 로그인 가능
- 랜딩에서 로그인 유도 가능
- 10단계 스테이지 플레이 가능
- 포즈 유사도 점수 표시 가능
- 순차 해금 동작
- 밈 오버레이 결과 연출 가능
- 15초 결과 영상 미리보기/업로드 가능
- 게시물 생성 가능
- 최신순/인기순 피드 가능
- 댓글/좋아요/팔로우 가능
- 프로필에 핵심 정보 노출 가능

## 11. 가장 현실적인 운영 팁

- 처음부터 10단계를 완벽하게 만들려고 하지 말고, 1단계 완성 후 복제 가능한 구조를 먼저 만든다.
- 피드는 처음엔 단순하게 만들고 추천 알고리즘은 뒤로 미룬다.
- 영상 업로드는 비용과 성능 이슈가 크므로 "사용자가 업로드 버튼을 눌렀을 때만 저장" 원칙을 유지한다.
- 랜딩보다 플레이와 업로드 경험을 먼저 고정하는 편이 전체 성공 확률이 높다.

## 12. 바로 다음 액션 추천

우선순위는 아래 순서가 가장 좋다.

1. Supabase DB 스키마 초안 작성
2. 앱 라우트 구조 정의
3. 스테이지 데이터 구조 설계
4. 포즈 유사도 점수 함수 설계
5. 결과 영상 녹화 방식 검증
6. 그 다음에 실제 프로젝트 생성
